<%= simple_form_for [ @restaurant, @review ] do |f| %>
  <%= f.input :content %>
  <%= f.input :rating %>
  <%= f.submit "Submit review", class: "btn btn-primary" %>
<% end %>

<p></p>

<%= link_to 'Back to Restaurant Infos', restaurant_path(@restaurant), class: "btn btn-primary" %>
